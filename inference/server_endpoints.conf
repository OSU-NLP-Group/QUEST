# Main model endpoints config.
# react_agent.py reads this file on every service call, so changes take effect at runtime.
# Format:
#   HOSTNAME_LIST=host1,host2,host3
#   PORTS=6000,6001,6002,6003

HOSTNAME_LIST=localhost
PORTS=6000
