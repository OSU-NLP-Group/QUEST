# Search service hot-reload config (re-read on every search call).
# Edit this file at runtime to switch the search service URL without restart.
#
# Format:
#   url=http://hostname:port
#
# You can also set SEARCH_NODES_CONF env var to point to a different file,
# or SEARCH_SERVICE_URL env var / tools.yaml search_service_url for a static URL.

url=http://a0011:8000
